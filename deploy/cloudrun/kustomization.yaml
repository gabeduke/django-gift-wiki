apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cloud Run deployment profile
# The app itself is deployed via Terraform (terraform/) and GitHub Actions.
# This kustomization manages cluster-side supporting resources (e.g. external-dns).
#
# Apply with: kubectl apply -k deploy/cloudrun

resources:
  - dns-endpoint.yaml
